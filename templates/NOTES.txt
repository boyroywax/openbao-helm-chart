🔐 OpenBao Vault Helm Chart - {{ .Chart.Version }}

Thank you for installing {{ .Chart.Name }}!
Your OpenBao Vault server has been deployed with the following configuration:

📋 Deployment Information:
• Release Name: {{ .Release.Name }}
• Namespace: {{ include "openbao.namespace" . }}
• Chart Version: {{ .Chart.Version }}
• App Version: {{ .Chart.AppVersion | default "latest" }}

🌐 Access Information:
{{- if .Values.ingress.enabled }}
• External URL: https://{{ include "openbao.hostname" . }}
• TLS Certificate: {{ include "openbao.tlsSecretName" . }}
{{- else }}
• Internal Service: {{ include "openbao.fullname" . }}.{{ include "openbao.namespace" . }}.svc.cluster.local:{{ .Values.service.port }}
• Access via port-forward:
  kubectl port-forward -n {{ include "openbao.namespace" . }} svc/{{ include "openbao.fullname" . }} 8200:{{ .Values.service.port }}
{{- end }}

💾 Storage Information:
{{- if .Values.persistence.enabled }}
• NFS Storage: Enabled ({{ .Values.persistence.size }})
• Storage Class: {{ .Values.persistence.storageClass }}
• NFS Server: {{ include "openbao.nfsServerName" . }}
{{- else }}
• Storage: Ephemeral (data will be lost on pod restart)
{{- end }}

🚀 Getting Started:

1. Check deployment status:
   kubectl get pods -n {{ include "openbao.namespace" . }}

2. View vault logs:
   kubectl logs -n {{ include "openbao.namespace" . }} deployment/{{ include "openbao.fullname" . }}

3. Access the Vault UI:
{{- if .Values.ingress.enabled }}
   Open: https://{{ include "openbao.hostname" . }}
{{- else }}
   kubectl port-forward -n {{ include "openbao.namespace" . }} svc/{{ include "openbao.fullname" . }} 8200:{{ .Values.service.port }}
   Then open: http://localhost:8200
{{- end }}

4. Initial setup (if using dev mode):
{{- if .Values.vault.env.VAULT_DEV_ROOT_TOKEN_ID }}
   Root Token: {{ .Values.vault.env.VAULT_DEV_ROOT_TOKEN_ID }}
{{- else }}
   # Initialize the vault (production mode)
   kubectl exec -n {{ include "openbao.namespace" . }} deployment/{{ include "openbao.fullname" . }} -- vault operator init
{{- end }}

🔍 Troubleshooting:

• Check pod status:
  kubectl describe pod -n {{ include "openbao.namespace" . }} -l app.kubernetes.io/name={{ include "openbao.name" . }}

• Check NFS server status:
  kubectl get pods -n {{ include "openbao.namespace" . }} -l app=nfs-server

• Check PVC status:
  kubectl get pvc -n {{ include "openbao.namespace" . }}

• Check PV status:
  kubectl get pv {{ .Release.Name }}-nfs-pv

📚 Documentation:
• Vault Documentation: https://developer.hashicorp.com/vault/docs
• OpenBao Documentation: https://openbao.org/docs
• Chart Repository: https://github.com/boyroywax/openbao-helm-chart

---
Happy Vaulting! 🔐✨